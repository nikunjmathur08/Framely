# Environment Variables

# Frontend (optional - defaults will work for local development)
VITE_BACKEND_URL=http://localhost:3001

# Backend (required - create server/.env with these values)
# See server/.env.example for backend configuration

VITE_STREAM_PROVIDER_URL=https://dlive.sx
